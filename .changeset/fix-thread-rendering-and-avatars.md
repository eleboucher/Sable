---
default: patch
---

Fix thread messages so mentions use nicknames, inline images keep their sizing, and sender profiles come from the cache instead of rendering blank. Room avatars now load in the Tauri desktop and Android apps.
